# CONTEXT — ChosenOne 点名器领域词汇表

| 术语 | 定义 |
|---|---|
| **ChosenOne / 天选之子** | 本项目的桌面点名器软件。界面单一窗口，核心为滚动抽奖式随机点名。 |
| **名单（name list）** | 可被抽取的全部名字集合。数据源为与可执行文件同目录的 `names.txt`。 |
| **排除名单（exclude list）** | 被排除的名字集合，其中的名字不参与抽取。数据源为与应用同目录的 `excluded.txt`，与应用中的名字重叠时取排除优先。 |
| **抽取（draw）** | 从当前可抽名字中随机选一人的操作。点击主按钮触发，伴随滚动动画。 |
| **滚动（roll）** | 抽取时的视觉动画：名字区快速轮换随机名字约 1.5 秒后减速停驻。 |
| **老虎机视口（slot viewport）** | 界面中央的名字滚动窗口：3 行 × 80px，抽取时名字在其中纵向滚动，停驻时中央行高亮。 |
| **已点名（picked）** | 本次会话中已被抽取过的人。抽取时自动跳过。 |
| **重置（reset）** | 将"已点名"集合清空，使全部名单重新可抽。 |
| **界面方案（scheme）** | 整体界面风格，`mist`（晨雾林窗，默认）/ `chalk`（绿板粉笔·木质课堂），设置面板切换、存 `settings.json`（旧文件缺省归一化为 mist）。决定配色变量族、字体、圆角与背景动效元素。 |
| **主题（theme）** | 同一界面方案下的配色明暗：`dark` / `light`，由 CSS 变量驱动。两方案各有双主题。 |
| **背景（background）** | 窗口背景。可为默认纯色或用户放在应用目录的本地图片。 |
| **背景动效层（bg-fx layer）** | 装饰性背景动画层（`.bg-fx`）：位于内容之下（z-index 0 vs 内容 1）、`pointer-events:none` 不拦交互、不参与布局（`position:absolute`）。元素低透明（≤0.7）、慢速（周期≥8s）、数量克制（≤8），中央老虎机视口区尽量留空；系统偏好减少动效（`prefers-reduced-motion: reduce`）时自动静止。晨雾林窗 = 晨雾漂移 + 晨光呼吸（3 团雾+光）；绿板粉笔 = 粉笔尘埃慢浮 + 暖木尘光（6 粒尘+光）。 |
| **设置（settings）** | `settings.json` 中持久化的配置（界面方案、主题、背景文件）。 |
| **应用目录（app dir）** | 可执行文件所在目录。`names.txt`、`excluded.txt`、`settings.json`、背景图片均位于此。 |
| **布局等比缩放（layout scale）** | 窗口任意尺寸下，整个 UI（含老虎机/字号/按钮）按 `min(宽/800, 高/500)` 等比 `zoom` 缩放，范围 0.7~1.15；基准 = 窗口默认尺寸 800×500（`tauri.conf.json` 同步）。仅渲染缩放：DOM 逻辑尺寸与动画几何（行高 80px / 视口 240px / 条带 42 行）不受影响。 |